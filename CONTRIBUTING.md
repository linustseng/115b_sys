# 貢獻與變更規範（115b_sys）

本專案採 **main 為正式分支**，預設以 **PR（Pull Request）** 方式合併。

## 1) 分支與流程

- 預設流程：`feature/*` 或 `fix/*` → PR → merge 到 `main`
- 緊急修復（hotfix）：允許直接修 `main`，但需在當日補上變更說明（commit 訊息或備註）

建議分支命名：

- `feature/<功能名稱>`
- `fix/<問題名稱>`
- `chore/<維運項目>`

---

## 2) Commit 訊息格式（可用中文）

格式：

`類型(範圍): 摘要`

範例：

- `新增(報名): 管理員手動報名並記錄操作日誌`
- `修正(活動): 修復時區轉換導致顯示錯誤`
- `重構(前端): 拆分活動表單驗證邏輯`
- `文件(部署): 補充 Vercel 回滾步驟`

建議類型：

- `新增`
- `修正`
- `重構`
- `文件`
- `測試`
- `維運`

撰寫原則：

- 摘要一句話說清楚「改了什麼」
- 盡量避免「更新」、「調整」這種過於模糊詞
- 一次 commit 盡量聚焦單一目的

---

## 3) PR 規範

PR 標題建議同 commit 風格：

`類型(範圍): 摘要`

PR 內容至少包含：

1. **目的**：這次為什麼要改
2. **變更內容**：改了哪些檔案與行為
3. **驗證方式**：怎麼確認功能正常（操作步驟）
4. **風險與回滾**：若出問題如何退回

---

## 4) 部署與風險控制（Vercel）

- 合併前至少確認：
  - 前端可 build
  - 關鍵流程可操作（登入、活動列表、報名/簽到、後台基本功能）
- 影響環境變數時，需在 PR 註明：
  - 新增/變更的 env key
  - 是否需要同步到 Vercel

---

## 5) 禁止事項

- 不要將密鑰、Token、帳密直接 commit 到 repo
- 不要在未確認影響範圍下直接改動正式資料結構
- 不要把多個不相關主題混在同一個 PR

---

## 6) 建議小習慣

- 小步快跑：小 PR、可回滾
- 每次修改都附最小驗證步驟
- 發現技術債時，先記錄再安排，不要悶著不說

---

如無特別說明，以上規範適用於所有前後端變更。